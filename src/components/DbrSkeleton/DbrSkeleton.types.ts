export interface DbrSkeletonProps {
  /**
   * Skeleton width (any valid CSS length).
   * @default "100%"
   */
  width?: string;
  /**
   * Skeleton height (any valid CSS length).
   * @default "0"
   */
  height?: string;
  /**
   * Skeleton border radius (any valid CSS radius).
   * @default "0"
   */
  radius?: string;
}
